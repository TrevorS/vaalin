// ABOUTME: Preview state for MainView with default layout (hands/vitals left, compass/spells right)

import SwiftUI

#Preview("Default Layout") {
    MainView()
        .frame(width: 1200, height: 800)
}
