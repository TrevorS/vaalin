// ABOUTME: Preview state for StreamsBarView with compact height (120pt)

import SwiftUI

#Preview("Compact Height (120pt)") {
    StreamsBarView(height: 120)
        .frame(width: 800)
        .padding()
}
