// ABOUTME: Preview state for StreamsBarView with standard height (200pt)

import SwiftUI

#Preview("Standard Height (200pt)") {
    StreamsBarView(height: 200)
        .frame(width: 800)
        .padding()
}
